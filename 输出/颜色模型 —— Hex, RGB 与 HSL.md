---
type: permanent
banner: Assets/Banner/pexels-faikackmerd-1025469.jpg
banner-x: 53
banner-y: 100
aliases:
  - Hex
  - RGB
  - HSL
---
# 🌐 核心观点

Hex, RGB 和 HSL 是 CSS 中三种常见的颜色表示方式, 分别基于不同的颜色模型, 便于开发者在不同场景下精确控制网页颜色.

---

# 🔖 详细解释

## 1	Hex 格式

Hex 格式使用十六进制来表示 RGB 值, 具体的格式为: `#RRGGBB`.

结构紧凑, 是设计工具和 CSS 中的默认选择.

## 2	RGB 格式

直接指定红黄蓝三原色的强度, 在 CSS 中的格式为: `rgb(R, G, B)` 或 带透明度的 `rgba(R, G, B, A)`.

常用于动态计算颜色或者需要精准通道控制的场景, 如渐变: `background: linear-gradient(to right, rgb(255,0,0), rgb(0,255,0));`.

## 3	HSL 格式

HSL 格式基于人类感知的色相 Hue, 饱和度 Saturation, 明度 Lightness, 格式为 `hsl(H, S%, L%)`.

开发中常用 HSL 生成主题切换或 hover 效果, 例如通过调整 L% 创建深浅变体 `.btn { background: hsl(200, 70%, 50%); } .btn:hover { background: hsl(200, 70%, 40%); }`.

---

# 📚 参考内容

