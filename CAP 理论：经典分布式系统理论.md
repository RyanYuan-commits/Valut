---
type: permanent
banner: 附件/Banner/pexels-vadimsadovski-33522604-7049988.jpg
aliases:
  - CAP
---
---

**关键词**: 分布式，CAP

---

## 1	什么是 CAP 理论？

![[CAP 理论.png|400]]

 CAP 理论指的是：对于一个系统，一致性，可用性和分区容错性只能满足其二。

- 一致性（Consistency）：强调数据的正确性，每次读操作，要么读到最新的数据，要么失败，要求整个分布式系统像一个不可拆分的整体，写操作具有原子性。
- 可用性（Availability）：强调服务的使用体验，每次请求不会失败且不会等待时间过长。
- 分区容错性（Partition Tolerance）：在网络不可靠的背景下，系统不会出现不可用的情况。

对于分布式系统而言，P 是必须要保证的，由此也就诞生了 CP 和 AP 两个流派，CP 保证从系统中读取到的数据总是正确的，但会在可用性上做出妥协，AP 保障系统总是可用，但是读取到的数据不总是准确。

一致性和可用性之间的矛盾在于网络环境的不确定性，下面对这两项涉及的问题做出探讨。

## 2	一致性问题

### 2.1	即时一致性问题

倘若 x 的初始值为 2，客户端通过 master 节点写入了 x=3，然后客户端请求 follower 节点获取 x 的值，如果 master 为了返回速度，在 follower 节点写入完成前就将写入成功的结果返回给客户端，此时客户端请求 follower 节点得到的就是旧值，这就是即时一致性问题。

### 2.2	顺序一致性问题

客户端向 master 节点发送两个请求，分别是 x=3 和 x=4，倘若在 master 同步第一条指令的时候发生了网络错误导致 x=4 更早的到达 follower，就会出现顺序一致性的问题，这个问题要比即使一致性严重的多，因为 follower 已经记录了错误的数据，无论何时都会返回错误的结果，这种情况下我们称之为分布式系统的**最终一致性**也遭到了破坏。

## 3	可用性问题

为了避免一致性问题，最好的方式就是采用串行化的方式，即 master 同步所有的 follower 节点后，等待所有 follower 写入完成，返回正确结果后再响应客户。

但这样会带来两个问题：

- 如果某一个 follower 出现宕机，无法响应 master，会将单机不可用放大到集群不可用；
- 如果某一台 follower 响应时间过长，会体现在整个集群响应时间过长，即木桶效应。

---